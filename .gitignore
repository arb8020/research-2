# Python bytecode and cache directories
__pycache__/
*.py[cod]

# Local Claude Code state and worktrees
.claude/
worktrees/

# Python build output (uv build / hatchling)
/dist/

# Built Vite output (produced by the hatchling build hook, see hatch_build.py)
mimir/ui/dist/
mimir/ui/app/node_modules/

# Coverage artifacts
.coverage
coverage.json

# Locally generated dependency lockfile
uv.lock

# External reference repos (not vendored)
ext/
